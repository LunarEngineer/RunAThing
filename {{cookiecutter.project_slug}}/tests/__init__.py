"""This demonstrates a reusable pattern.

This structure is reused by pytest when building testing
expectations.

Tl;Dr: Make sure your python files are named 'test_thing' and the
functions which run your tests are all called 'test_whatever'.
That will ensure that pytest can pick them and run them.
"""