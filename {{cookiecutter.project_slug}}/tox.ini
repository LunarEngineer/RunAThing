###AutoGeneratedDocument
# This file is automatically generated.
# Rerunning the packaging script will not respect your changes.
# If you wish to implement change do that within your config.yaml.

[tox]
requires =
    tox>=4
env_list = lint, type, py{ {cookiecutter.min_version}}-{ {cookiecutter.max_version}}

[testenv]
description = Setup for the testing suite.

commands =
    pytest {posargs:tests}
[testenv:lint]
description = run linters
skip_install = true
deps =
    black==22.12
commands = black {posargs:.}

[testenv:type]
description = run type checks
deps =
    mypy>=0.991
commands =
    mypy {posargs:src tests}
